import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    searchBar: {
        alignSelf:'center',
        height: 40,
        width: 300,
        borderColor: 'black',
        borderWidth: 1,
        marginTop: 20,
        borderRadius: 8,
        textAlign: 'center',
        marginBottom: 40,
    },
});
export {Styles};